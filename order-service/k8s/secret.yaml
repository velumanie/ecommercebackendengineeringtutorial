# Placeholder only — in dev/qa this is applied directly; in uat/prod this Secret is
# populated by the platform's external secrets operator (Vault/Secrets Manager), never
# committed with real values.
apiVersion: v1
kind: Secret
metadata:
  name: order-service-secret
  namespace: ecommerce
type: Opaque
stringData:
  DB_PASSWORD: "change-me"
  JWT_SECRET: "change-me-in-every-environment-this-is-dev-only-do-not-use-in-production-0123456789"
