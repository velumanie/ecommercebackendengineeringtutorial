apiVersion: v1
kind: Secret
metadata:
  name: postgres-products-secret
  namespace: data
type: Opaque
stringData:
  POSTGRES_DB: products_db
  POSTGRES_USER: products_user
  POSTGRES_PASSWORD: change-me
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-products
  namespace: data
spec:
  serviceName: postgres-products
  replicas: 1
  selector: { matchLabels: { app: postgres-products } }
  template:
    metadata: { labels: { app: postgres-products } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports: [{ containerPort: 5432 }]
          envFrom: [{ secretRef: { name: postgres-products-secret } }]
          volumeMounts:
            - { name: data, mountPath: /var/lib/postgresql/data }
          resources:
            requests: { cpu: "500m", memory: "1Gi" }
            limits: { cpu: "2", memory: "4Gi" }
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "products_user"] }
            periodSeconds: 10
          livenessProbe:
            exec: { command: ["pg_isready", "-U", "products_user"] }
            periodSeconds: 20
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ReadWriteOnce]
        resources: { requests: { storage: 50Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-products
  namespace: data
spec:
  clusterIP: None
  selector: { app: postgres-products }
  ports:
    - port: 5432
      targetPort: 5432
