apiVersion: v1
kind: Secret
metadata:
  name: postgres-users-secret
  namespace: data
type: Opaque
stringData:
  POSTGRES_DB: users_db
  POSTGRES_USER: users_user
  POSTGRES_PASSWORD: change-me
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-users
  namespace: data
spec:
  serviceName: postgres-users
  replicas: 1
  selector: { matchLabels: { app: postgres-users } }
  template:
    metadata: { labels: { app: postgres-users } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports: [{ containerPort: 5432 }]
          envFrom: [{ secretRef: { name: postgres-users-secret } }]
          volumeMounts:
            - { name: data, mountPath: /var/lib/postgresql/data }
          resources:
            requests: { cpu: "500m", memory: "1Gi" }
            limits: { cpu: "2", memory: "4Gi" }
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "users_user"] }
            periodSeconds: 10
          livenessProbe:
            exec: { command: ["pg_isready", "-U", "users_user"] }
            periodSeconds: 20
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ReadWriteOnce]
        resources: { requests: { storage: 50Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-users
  namespace: data
spec:
  clusterIP: None
  selector: { app: postgres-users }
  ports:
    - port: 5432
      targetPort: 5432
