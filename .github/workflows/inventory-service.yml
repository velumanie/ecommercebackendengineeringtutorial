name: inventory-service CI

on:
  push:
    branches: [main, develop]
    paths:
      - "inventory-service/**"
      - "common/**"
      - ".github/workflows/inventory-service.yml"
  pull_request:
    paths:
      - "inventory-service/**"
      - "common/**"

jobs:
  build-test-analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: "25" }

      - name: Build
        run: ./gradlew :inventory-service:compileJava --no-daemon

      - name: Unit + integration tests
        run: ./gradlew :inventory-service:test --no-daemon

      - name: SonarQube analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: ./gradlew :inventory-service:sonar --no-daemon -Dsonar.host.url=${{ vars.SONAR_HOST_URL }}
