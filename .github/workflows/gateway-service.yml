name: gateway-service CI

on:
  push:
    branches: [main, develop]
    paths:
      - "gateway-service/**"
      - "common/**"
      - ".github/workflows/gateway-service.yml"
  pull_request:
    paths:
      - "gateway-service/**"
      - "common/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: "25" }

      - name: Build
        run: ./gradlew :gateway-service:compileJava --no-daemon

      - name: Unit + integration tests
        run: ./gradlew :gateway-service:test --no-daemon
